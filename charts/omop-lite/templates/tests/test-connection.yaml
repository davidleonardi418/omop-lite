apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "omop-lite.fullname" . }}-test-connection"
  labels:
    {{- include "omop-lite.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "omop-lite.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
