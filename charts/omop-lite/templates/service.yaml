apiVersion: v1
kind: Service
metadata:
  name: {{ include "omop-lite.fullname" . }}
  labels:
    {{- include "omop-lite.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "omop-lite.selectorLabels" . | nindent 4 }}
